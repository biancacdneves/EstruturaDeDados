def shell_sort(lista, chave=None, reverso=False): n = len(lista) gap = n // 2 while gap > 0: for i in range(gap, n): temp = lista[i] j = i while j >= gap and ((lista[j - gap][chave] > temp[chave]) if chave else (lista[j - gap] > temp)): if reverso: if (lista[j - gap][chave] < temp[chave]) if chave else (lista[j - gap] < temp): break lista[j] = lista[j - gap] j -= gap lista[j] = temp gap //= 2 return lista 
